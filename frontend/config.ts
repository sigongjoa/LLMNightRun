// Re-export constants from utils/constants.ts
import { API_BASE_URL, APP_NAME, APP_VERSION, DEFAULT_TIMEOUT } from './utils/constants';

export {
  API_BASE_URL,
  APP_NAME,
  APP_VERSION,
  DEFAULT_TIMEOUT
};

// Add any additional configuration variables here
